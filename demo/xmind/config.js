const defaultConfig = {
    // 节点配置
    node: {
        padding: 20,
        fontSize: {
            root: 30,
            normal: 14
        },
        backgroundColor: {
            root: '#1a237e',
            normal: '#eee'
        },
        color: {
            root: '#fff',
            normal: '#000'
        },
        maxWidth: 200,
        borderRadius: 4,
        borderWidth: 1,
        borderColor: '#ccc',
    },
    // 连线配置
    line: {
        stroke: '#666',
        strokeWidth: 2,
        cornerRadius: 8
    },
    // 布局配置
    layout: {
        groupGap: 0,
        verticalGap: 10,
        horizontalGap: 100
    }
}

export default defaultConfig