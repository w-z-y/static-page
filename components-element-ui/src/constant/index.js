export const DEFAULT_PROPS = {
    label: 'label',
    value: 'value',
    children: 'children',

    // disabled:    boolean || function(data){}     (el-tree)
    // showLabel:   string                          (dropdown)
}